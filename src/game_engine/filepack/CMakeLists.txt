FILE (GLOB_RECURSE FILEPACK_SOURCES *.cpp *.c)
INCLUDE_DIRECTORIES (${newzfs_engine_SOURCE_DIR}/head/filepack)
INCLUDE_DIRECTORIES (${newzfs_engine_SOURCE_DIR}/head/core)
ADD_LIBRARY(filepack_${CMAKE_BUILD_TYPE} ${FILEPACK_SOURCES})
